# ressactor

A very simple flask app, which enables users to add comments on posts in an RSS feed.

## Running

Just add the RSS feed at `instance/feed.xml`, and then run the app like a normal flask app:

```sh
pip install -r requirements.txt
flask --app ressactor run
```
